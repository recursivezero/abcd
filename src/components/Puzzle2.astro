---
import Settings from "@/components/Settings.astro";
import HintIcon from "@/assets/icons/hint.svg";
---

<div class="puzzle-container">
  <Settings />
  <details open>
    <summary>Word List</summary>
    <div class="word-list" id="wordList"></div>
  </details>

  <div class="stats-container">
    <div class="stats-button">
      <span>Clicks: <span id="clickCount">0</span></span>
    </div>
    <div class="stats-button">
      <span>Found: <span id="foundCount">0</span>/<span id="totalWords"></span></span>
    </div>
    <button id="shuffle" class="action-btn btn-shuffle">🔀 Shuffle</button>
    <button id="new" class="action-btn btn-new">✨ New Puzzle</button>
    <button id="solutionBtn" class="hint-button" aria-label="Toggle hints">
      <div style="width: 28px; height: 28px;">
        <HintIcon />
      </div>
    </button>
    <button id="main-clear" class="action-btn btn-clear">🗑️ Clear</button>
  </div>

  <div id="grid" class="grid__puzzle"></div>
</div>

<script is:inline>
  var WORD_SETS = [
    // Set 1 — Animals
    {
      easy: ["CAT", "DOG", "SUN", "MOON", "STAR"],
      medium: ["APPLE", "GRAPE", "ORANGE", "BERRIES"],
      hard: ["WALRUS", "GIRAFFE", "PENGUIN", "DOLPHIN"]
    },
    // Set 2 — Household
    {
      easy: ["HAT", "CAR", "BALL", "SHOE", "LAMP"],
      medium: ["CHERRY", "COCONUT", "PLUM", "MELON", "PEACH"],
      hard: ["OCTOPUS", "JACKAL", "WALRUS", "PEACOCK"]
    },
    // Set 3 — Colors
    {
      easy: ["RED", "BLUE", "GREEN", "YELLOW", "PURPLE"],
      medium: ["BLACK", "WHITE", "BROWN", "ORANGE", "PINK"],
      hard: ["MAGENTA", "CYAN", "VIOLET", "MAROON"]
    },
    // Set 4 — Space
    {
      easy: ["SUN", "MARS", "MOON", "STAR", "COMET"],
      medium: ["VENUS", "EARTH", "PLUTO", "ORBIT", "SOLAR"],
      hard: ["JUPITER", "MERCURY", "NEPTUNE", "SATURN"]
    },
    // Set 5 — Food
    {
      easy: ["EGG", "MILK", "RICE", "CAKE", "SOUP"],
      medium: ["BREAD", "SUGAR", "CREAM", "PASTA", "SPICE"],
      hard: ["COOKIE", "BUTTER", "NOODLE", "MUFFIN"]
    },
    // Set 6 — Sports
    {
      easy: ["RUN", "JUMP", "SWIM", "KICK", "BOWL"],
      medium: ["CHESS", "RUGBY", "DERBY", "SQUASH", "POLO"],
      hard: ["CRICKET", "TENNIS", "HOCKEY", "BOXING"]
    },
    // Set 7 — Body
    {
      easy: ["ARM", "LEG", "EYE", "HAND", "KNEE"],
      medium: ["HEART", "BRAIN", "SPINE", "NERVE", "LIVER"],
      hard: ["MUSCLE", "KIDNEY", "STOMACH", "THROAT"]
    },
    // Set 8 — Travel
    {
      easy: ["MAP", "ROAD", "CITY", "TOWN", "PARK"],
      medium: ["HOTEL", "TRAIN", "PLANE", "BEACH", "RIVER"],
      hard: ["AIRPORT", "STATION", "HIGHWAY", "VILLAGE"]
    },
    // Set 9 — School
    {
      easy: ["PEN", "BOOK", "DESK", "BELL", "TEST"],
      medium: ["CLASS", "GRADE", "STUDY", "ESSAY", "NOTES"],
      hard: ["PENCIL", "LESSON", "SCIENCE", "HISTORY"]
    },
    // Set 10 — Weather
    {
      easy: ["SUN", "RAIN", "WIND", "SNOW", "HAIL"],
      medium: ["CLOUD", "STORM", "FROST", "FOGGY", "SLEET"],
      hard: ["THUNDER", "RAINBOW", "BLIZZARD", "CYCLONE"]
    }
  ];

  var COLOR_CLASSES = [
    "word-color-1",
    "word-color-2",
    "word-color-3",
    "word-color-4",
    "word-color-5",
    "word-color-6",
    "word-color-7",
    "word-color-8"
  ];

  var currentWordSetIndex = 0;
  var state = {
    difficulty: "easy",
    grid: [],
    size: 8,
    selectedCells: [],
    foundWords: [],
    isDragging: false,
    showSolution: false,
    solutions: [],
    clickCount: 0,
    matchedCells: new Map(),
    currentWordSet: WORD_SETS[0],
    solutionColors: new Map(),
    wordColors: new Map()
  };

  function randomLetter() {
    return String.fromCharCode(65 + Math.floor(Math.random() * 26));
  }

  function randomColor() {
    return "hsla(" + Math.floor(Math.random() * 360) + ",85%,72%,0.75)";
  }

  function getWordDirection(cells) {
    var r1 = cells[0][0],
      c1 = cells[0][1];
    var r2 = cells[cells.length - 1][0],
      c2 = cells[cells.length - 1][1];
    return r1 === r2 ? "horizontal" : c1 === c2 ? "vertical" : "diagonal";
  }

  function isValidLine(cells) {
    if (cells.length < 2) return true;
    var dr = cells[1][0] - cells[0][0],
      dc = cells[1][1] - cells[0][1];
    if (Math.abs(dr) > 1 || Math.abs(dc) > 1 || (dr === 0 && dc === 0)) return false;
    for (var i = 2; i < cells.length; i++) {
      if (cells[i][0] !== cells[0][0] + dr * i || cells[i][1] !== cells[0][1] + dc * i) return false;
    }
    return true;
  }

  function canPlace(grid, word, row, col, dr, dc) {
    var lr = row + dr * (word.length - 1),
      lc = col + dc * (word.length - 1);
    if (lr < 0 || lr >= state.size || lc < 0 || lc >= state.size) return false;
    for (var i = 0; i < word.length; i++) {
      var cur = grid[row + dr * i][col + dc * i];
      if (cur !== "" && cur !== word[i]) return false;
    }
    return true;
  }

  function placeWord(grid, word) {
    var dirs = [
      [0, 1],
      [1, 0],
      [1, 1],
      [1, -1]
    ];
    for (var a = 0; a < 200; a++) {
      var d = dirs[Math.floor(Math.random() * dirs.length)];
      var row = Math.floor(Math.random() * state.size);
      var col = Math.floor(Math.random() * state.size);
      if (canPlace(grid, word, row, col, d[0], d[1])) {
        var wordCells = [];
        for (var i = 0; i < word.length; i++) {
          var r = row + d[0] * i,
            c = col + d[1] * i;
          grid[r][c] = word[i];
          wordCells.push([r, c]);
        }
        return { placed: true, cells: wordCells };
      }
    }
    return { placed: false, cells: [] };
  }

  function generatePuzzle() {
    var newGrid = [];
    for (var i = 0; i < state.size; i++) {
      newGrid.push([]);
      for (var j = 0; j < state.size; j++) newGrid[i].push("");
    }

    var words = state.currentWordSet[state.difficulty];
    state.solutions = [];
    state.wordColors = new Map();
    state.matchedCells = new Map();
    state.solutionColors = new Map();

    for (var w = 0; w < words.length; w++) {
      var res = placeWord(newGrid, words[w]);
      if (res.placed) {
        state.solutions.push({ word: words[w], cells: res.cells });
        state.solutionColors.set(words[w], randomColor());
      }
    }

    for (var i = 0; i < state.size; i++)
      for (var j = 0; j < state.size; j++) if (newGrid[i][j] === "") newGrid[i][j] = randomLetter();

    state.grid = newGrid;
    state.foundWords = [];
    state.showSolution = false;
    state.selectedCells = [];
    state.clickCount = 0;
    renderDOM();
  }

  function newPuzzle() {
    currentWordSetIndex = (currentWordSetIndex + 1) % WORD_SETS.length;
    state.currentWordSet = WORD_SETS[currentWordSetIndex];
    generatePuzzle();
  }

  function isValidNext(a, b) {
    return Math.abs(a[0] - b[0]) <= 1 && Math.abs(a[1] - b[1]) <= 1;
  }

  function checkWord(selection) {
    if (selection.length < 3 || !isValidLine(selection)) return false;
    var word = selection
      .map(function (rc) {
        return state.grid[rc[0]][rc[1]];
      })
      .join("");
    var rev = word.split("").reverse().join("");
    var list = state.currentWordSet[state.difficulty];
    var found = null;
    if (list.indexOf(word) >= 0 && state.foundWords.indexOf(word) < 0) found = word;
    else if (list.indexOf(rev) >= 0 && state.foundWords.indexOf(rev) < 0) found = rev;
    if (found) {
      state.foundWords.push(found);
      if (!state.wordColors.has(found))
        state.wordColors.set(found, COLOR_CLASSES[state.wordColors.size % COLOR_CLASSES.length]);
      state.matchedCells.set(found, selection.slice());
      return true;
    }
    return false;
  }

  function getCoords(el) {
    if (!el || !el.dataset || !el.dataset.coords) return null;
    return el.dataset.coords.split("-").map(Number);
  }

  function renderDOM() {
    var gridEl = document.getElementById("grid");
    if (!gridEl) return;

    var isMobile = window.innerWidth <= 768;
    var isLandscape = window.innerWidth > window.innerHeight;

    var gridSize, cellSize;
    if (isMobile) {
      if (isLandscape) {
        gridSize = window.innerHeight - 16;
      } else {
        gridSize = window.innerWidth - 16;
      }
      cellSize = Math.floor((gridSize - 12 - 7 * 3) / 8);

      gridEl.style.display = "grid";
      gridEl.style.gridTemplateColumns = "repeat(8, " + cellSize + "px)";
      gridEl.style.gridTemplateRows = "repeat(8, " + cellSize + "px)";
      gridEl.style.width = cellSize * 8 + 7 * 3 + 12 + "px";
      gridEl.style.height = cellSize * 8 + 7 * 3 + 12 + "px";
      gridEl.style.gap = "3px";
      gridEl.style.padding = "6px";
    } else {
      gridEl.style.display = "grid";
      gridEl.style.gridTemplateColumns = "repeat(8, 1fr)";
      gridEl.style.gridTemplateRows = "";
      gridEl.style.width = "";
      gridEl.style.height = "";
      gridEl.style.gap = "";
      gridEl.style.padding = "";
    }

    gridEl.innerHTML = "";

    for (var i = 0; i < state.grid.length; i++) {
      for (var j = 0; j < state.grid[i].length; j++) {
        var cell = document.createElement("div");
        cell.className = "cell";
        cell.textContent = state.grid[i][j];
        cell.dataset.coords = i + "-" + j;

        if (isMobile) {
          cell.style.width = cellSize + "px";
          cell.style.height = cellSize + "px";
          cell.style.fontSize = Math.floor(cellSize * 0.45) + "px";
          cell.style.display = "flex";
          cell.style.justifyContent = "center";
          cell.style.alignItems = "center";
          cell.style.boxSizing = "border-box";
          cell.style.border = "1.5px solid #e2e8f0";
          cell.style.borderRadius = "5px";
          cell.style.background = "#f8fafc";
          cell.style.fontWeight = "700";
          cell.style.color = "#1e293b";
          cell.style.cursor = "pointer";
          cell.style.userSelect = "none";
          cell.style.webkitUserSelect = "none";
          cell.style.touchAction = "none";
          cell.style.overflow = "hidden";
        }

        var iSel = false;
        for (var s = 0; s < state.selectedCells.length; s++) {
          if (state.selectedCells[s][0] === i && state.selectedCells[s][1] === j) {
            iSel = true;
            break;
          }
        }
        if (iSel) {
          cell.classList.add("selected");
          if (isMobile) {
            cell.style.background = "var(--primary-color)";
            cell.style.color = "#fff";
          }
        }

        state.matchedCells.forEach(function (cells, word) {
          for (var s = 0; s < cells.length; s++) {
            if (cells[s][0] === i && cells[s][1] === j) {
              cell.classList.add("match");
              var cc = state.wordColors.get(word);
              if (cc) cell.classList.add(cc);
              break;
            }
          }
        });

        if (state.showSolution) {
          for (var sol = 0; sol < state.solutions.length; sol++) {
            var sc = state.solutions[sol];
            for (var s = 0; s < sc.cells.length; s++) {
              if (sc.cells[s][0] === i && sc.cells[s][1] === j) {
                cell.style.backgroundColor = state.solutionColors.get(sc.word);
                break;
              }
            }
          }
        }

        gridEl.appendChild(cell);
      }
    }

    var clickCountEl = document.getElementById("clickCount");
    var foundCountEl = document.getElementById("foundCount");
    var totalWordsEl = document.getElementById("totalWords");
    var wordListEl = document.getElementById("wordList");
    var solutionBtnEl = document.getElementById("solutionBtn");

    if (clickCountEl) clickCountEl.textContent = state.clickCount;
    if (foundCountEl) foundCountEl.textContent = state.foundWords.length;
    if (totalWordsEl) totalWordsEl.textContent = state.currentWordSet[state.difficulty].length;

    if (wordListEl) {
      var words = state.currentWordSet[state.difficulty];
      var html = "";
      for (var w = 0; w < words.length; w++) {
        var word = words[w];
        var found = state.foundWords.indexOf(word) >= 0;
        var cc = state.wordColors.get(word) || "";
        var sc = state.showSolution ? state.solutionColors.get(word) : null;
        var style = sc ? ' style="background-color:' + sc + ';"' : "";
        html += '<span class="word ' + (found ? "found " + cc : "") + '"' + style + ">" + word + "</span>";
      }
      wordListEl.innerHTML = html;
    }

    if (solutionBtnEl) solutionBtnEl.classList.toggle("active", state.showSolution);
  }

  // ── Events ──
  function setupEvents() {
    var gridEl = document.getElementById("grid");
    if (!gridEl) return;

    gridEl.addEventListener("mousedown", function (e) {
      var coords = getCoords(e.target);
      if (!coords) return;
      state.isDragging = true;
      state.selectedCells = [coords];
      state.clickCount++;
      renderDOM();
    });

    gridEl.addEventListener("mousemove", function (e) {
      if (!state.isDragging) return;
      var coords = getCoords(e.target);
      if (!coords) return;
      var last = state.selectedCells[state.selectedCells.length - 1];
      if (!last || !isValidNext(last, coords)) return;
      var already = false;
      for (var s = 0; s < state.selectedCells.length; s++) {
        if (state.selectedCells[s][0] === coords[0] && state.selectedCells[s][1] === coords[1]) {
          already = true;
          break;
        }
      }
      if (!already) {
        state.selectedCells.push(coords);
        renderDOM();
      }
    });

    document.addEventListener("mouseup", function () {
      if (!state.isDragging) return;
      state.isDragging = false;
      if (!checkWord(state.selectedCells)) state.selectedCells = [];
      renderDOM();
    });

    gridEl.addEventListener(
      "touchstart",
      function (e) {
        e.preventDefault();
        var t = e.touches[0];
        var el = document.elementFromPoint(t.clientX, t.clientY);
        var coords = getCoords(el);
        if (!coords) return;
        state.isDragging = true;
        state.selectedCells = [coords];
        state.clickCount++;
        renderDOM();
      },
      { passive: false }
    );

    gridEl.addEventListener(
      "touchmove",
      function (e) {
        e.preventDefault();
        if (!state.isDragging) return;
        var t = e.touches[0];
        var el = document.elementFromPoint(t.clientX, t.clientY);
        var coords = getCoords(el);
        if (!coords) return;
        var last = state.selectedCells[state.selectedCells.length - 1];
        if (!last || !isValidNext(last, coords)) return;
        var already = false;
        for (var s = 0; s < state.selectedCells.length; s++) {
          if (state.selectedCells[s][0] === coords[0] && state.selectedCells[s][1] === coords[1]) {
            already = true;
            break;
          }
        }
        if (!already) {
          state.selectedCells.push(coords);
          renderDOM();
        }
      },
      { passive: false }
    );

    gridEl.addEventListener(
      "touchend",
      function (e) {
        e.preventDefault();
        if (!state.isDragging) return;
        state.isDragging = false;
        if (!checkWord(state.selectedCells)) state.selectedCells = [];
        renderDOM();
      },
      { passive: false }
    );

    // ── Null-safe button listeners ──

    var levelEl = document.getElementById("level");
    if (levelEl) {
      levelEl.addEventListener("change", function (e) {
        state.difficulty = e.target.value;
        generatePuzzle();
      });
    }

    var newEl = document.getElementById("new");
    if (newEl) newEl.addEventListener("click", newPuzzle);

    var shuffleEl = document.getElementById("shuffle");
    if (shuffleEl) shuffleEl.addEventListener("click", generatePuzzle);

    var solutionBtnEl = document.getElementById("solutionBtn");
    if (solutionBtnEl) {
      solutionBtnEl.addEventListener("click", function () {
        state.showSolution = !state.showSolution;
        renderDOM();
      });
    }

    var clearEl = document.getElementById("main-clear");
    if (clearEl) {
      clearEl.addEventListener("click", function () {
        state.selectedCells = [];
        state.foundWords = [];
        state.matchedCells = new Map();
        state.wordColors = new Map();
        state.solutionColors = new Map();
        state.isDragging = false;
        state.showSolution = false;
        renderDOM();
      });
    }

    var sNewEl = document.getElementById("s-new");
    if (sNewEl) sNewEl.addEventListener("click", newPuzzle);

    var sShuffleEl = document.getElementById("s-shuffle");
    if (sShuffleEl) sShuffleEl.addEventListener("click", generatePuzzle);
  }

  // ── Start ──
  generatePuzzle();
  setupEvents();

  // cross.json load karo background mein
  fetch("/assets/json/cross.json")
    .then(function (r) {
      return r.json();
    })
    .then(function (data) {
      if (Array.isArray(data) && data.length > WORD_SETS.length) {
        WORD_SETS = data;
        state.currentWordSet = WORD_SETS[0];
        generatePuzzle();
      }
    })
    .catch(function () {});
</script>
