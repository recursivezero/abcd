---
import "@/assets/styles/survey.css";
import SharePopover from "@/components/ShareButton.astro";
import SurveyComponent from "@/components/survey.astro";
import surveyJson from "@/data/survey.json";
import BaseLayout from "@/layouts/Base";
import type { SurveyData } from "@/types";

const meta = {
  title: "Survey Form",
  description: "Participate in our survey and help us improve your learning experience!"
};
---

<BaseLayout meta={meta}>
  <Fragment slot="header-right">
    <SharePopover />
  </Fragment>
  <SurveyComponent data={surveyJson as SurveyData} />
</BaseLayout>
